#include "rts.hpp"

/// this module exists solely to serve as a general 'lint' pass over
/// all of the headers to ensure everything compiles.

/// @cond PRIVATE
namespace rts {
  RTS_UNUSED int lint = 0;
}
/// @endcond
